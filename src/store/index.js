import Vue from 'vue';
import Vuex from 'vuex';
import metrics from './metrics.store'

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    metrics
  }
})
